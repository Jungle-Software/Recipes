import { Error } from "./SelectRecipeError.styles";

const SelectRecipeError = (props: any) => {
  return <Error>Please select a recipe (or insert a new one if you have none!)</Error>;
};

export default SelectRecipeError;
